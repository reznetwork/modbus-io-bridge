# /etc/default/modbus-io-bridge
# Optional overrides for the systemd service
# Path to python binary (use venv if you want)
PYTHON_BIN=/usr/bin/python3
# Default config file (unused by the @.service which sets per-instance files)
CONFIG=/opt/modbus-io-bridge/config.yaml
# Extra args for the script (e.g., logging overrides)
EXTRA_ARGS=
